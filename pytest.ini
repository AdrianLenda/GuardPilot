[pytest]
addopts = -q
testpaths =
    backend/tests
    apps/ui/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: marks tests as slow
    unit: unit tests
    integration: integration tests
